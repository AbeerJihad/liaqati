@model List<Product>



@if (Model.Count > 0)
{

    <div class="row row-cols-lg-2 p-0 g-0">
        @for (int i = 0; i < Model.Count; i++)
        {
            <partial name="_ProductCard" model="@Model[i]" />
        }
    </div>
}