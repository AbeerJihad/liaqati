@model List<Consultation>
@if (Model.Count > 0)
{

    <div class="row  row-cols-1 flex-column p-0 g-0">
        @foreach (var item in Model)
        {
            <partial name="_ConsultationSmallCard" model="item" />
        }

    </div>
}